#!/bin/bash

# PilotProOS Manager - Cross-Platform Interactive Menu
# Double-click to launch on macOS

# Change to script directory
cd "$(dirname "$0")"

# Use the Node.js version
exec node stack-manager.cjs