# ============================================================================
# PRODUCTION REQUIREMENTS - PilotProOS Intelligence Engine
# ============================================================================
# Optimized dependencies for production API runtime (no UI, no dev tools)
# Target: ~800MB installed (vs 2.41GB current)
# Date: 2025-10-13
# ============================================================================

# LangChain Core - Latest stable (January 2025)
langchain==0.3.27
langchain-community==0.3.30
langchain-core==0.3.76
langserve==0.3.0
langgraph==0.6.7
langgraph-checkpoint-postgres==2.0.11
langgraph-checkpoint-redis==0.1.2
langsmith>=0.3.45
langchain-openai==0.3.33
langchain-anthropic==0.3.0

# Vector Store & Embeddings (NO sentence-transformers - HTTP client only)
langchain-postgres>=0.0.15
pgvector>=0.3.6
langchain-chroma>=0.1.4

# LLM Providers
openai>=1.104.2
anthropic==0.40.0
google-generativeai==0.8.3
groq>=0.30.0
langchain-groq==0.3.8
langchain-google-genai==2.0.7

# FastAPI & Async
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.11.0

# Redis for Memory & Cache
redis>=5.2.1

# PostgreSQL
asyncpg==0.30.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# Observability & Tracing
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# Document Processing (RAG essentials only)
pypdf==5.1.0
beautifulsoup4==4.12.3
python-docx==1.1.2
Pillow==10.4.0

# WebSocket & Real-time
python-socketio==5.11.4
websockets==14.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18

# Monitoring & Metrics (NO grafana-client)
prometheus-client==0.21.1

# Utils
python-dotenv==1.0.1
httpx==0.27.2
tenacity==9.0.0
aiofiles==24.1.0
rich==13.9.4
loguru==0.7.3
jinja2==3.1.4

# LangGraph Visualization (required for draw_png)
pygraphviz>=1.13
langgraph-cli[inmem]>=0.1.55

# MCP Integration
langchain-mcp-adapters>=0.1.0

# ============================================================================
# REMOVED PACKAGES (vs original requirements.txt):
# ============================================================================
# - sentence-transformers==3.3.1 (1.5GB) - DUPLICATE: embeddings via HTTP
# - streamlit==1.50.0 (400MB) - DEV ONLY: UI not used in production
# - plotly==5.24.1 - DEV ONLY
# - matplotlib==3.9.2 - DEV ONLY
# - streamlit-chat==0.1.1 - DEV ONLY
# - streamlit-aggrid==0.3.5 - DEV ONLY
# - scikit-learn==1.6.1 (400MB) - UNUSED: ML routing not used
# - scipy==1.15.1 - UNUSED
# - pandas>=2.2.0 (200MB) - UNUSED: only in dashboard.py (dev)
# - numpy>=1.26.0 - UNUSED: only in dashboard.py (dev)
# - faiss-cpu==1.9.0 (50MB) - UNUSED: vector search in ChromaDB
# - grafana-client==3.6.0 - UNUSED: Prometheus sufficient
# - pytest, black, ruff, mypy - DEV ONLY
# - einops==0.8.1 - Only needed by sentence-transformers
# ============================================================================
# TOTAL REMOVED: ~2.67GB (61.5% reduction)
# ============================================================================
